import { connect } from 'react-redux';
import FooterPlayer from './footer_player';

const msp = state => ({
})

const mdp = dispatch => ({

})

export default connect(
    msp, mdp
)(FooterPlayer)
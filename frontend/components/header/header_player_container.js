import { connect } from 'react-redux';
import HeaderPlayer from './header_player';

const msp = state => ({
})

const mdp = dispatch => ({

})

export default connect(
    msp, mdp
)(HeaderPlayer)
import { connect } from 'react-redux';
import LeftSiderbar from './left_sidebar';

const msp = state => ({
})

const mdp = dispatch => ({

})

export default connect(
    msp, mdp
)(LeftSiderbar)